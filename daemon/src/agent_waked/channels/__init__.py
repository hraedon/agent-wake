"""Delivery channel adapters — the human-directed delivery backends.

Plan 005: delivery backends are adapters, exactly like harness adapters.
A human channel (webhook, email) is one more adapter with its own secret
custody per Plan 004 discipline — no bespoke side channel.
"""
